# Railway Environment Variables for Backend

# Database connection (use Railway's MySQL plugin reference)
# Example: ${{MySQL.DATABASE_URL}}
DATABASE_URL=mysql://user:password@host:3306/database

# AI Service URL (use Railway's internal URL)
# Example: http://ai-service.railway.internal:8000
AI_SERVICE_URL=http://ai-service.railway.internal:8000

# Node environment
NODE_ENV=production

# Port (Railway sets this automatically, do not override)
# PORT=3000
